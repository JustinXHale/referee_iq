import 'package:flutter/material.dart';

class SourcesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Optionally, you can remove the AppBar here if you want the header from SofiaScreen to remain consistent.
      body: Center(
        child: Text('Sources Content Here'),
      ),
    );
  }
}
